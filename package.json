{
  "name": "monorepo-amplify",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:pacificaa/monorepo-amplify.git",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "dev": "yarn workspace quizapp run dev",
    "start": "yarn workspace quizapp run start",
    "lint": "yarn workspaces lint"
  },
  "devDependencies": {
    "@aws-amplify/backend": "^1.5.1",
    "@aws-amplify/backend-cli": "^1.3.0",
    "aws-cdk": "^2.164.1",
    "aws-cdk-lib": "^2.164.1",
    "constructs": "^10.4.2",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "aws-amplify": "^6.6.7"
  }
}
